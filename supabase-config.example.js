// Copy this file to supabase-config.js and fill in your project (Dashboard → Settings → API).
// The anon key is safe in the browser if RLS only allows inserts you expect (see supabase/schema.sql).
window.SUPABASE_URL = "https://YOUR_PROJECT_REF.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR_ANON_PUBLIC_KEY";
