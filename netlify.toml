# Static site. Build writes supabase-config.js from SUPABASE_URL + SUPABASE_ANON_KEY
# (Netlify → Site configuration → Environment variables).
[build]
  command = "npm run build"
  publish = "public"
